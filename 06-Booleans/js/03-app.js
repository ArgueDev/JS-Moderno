const autenticado = true;

if (autenticado) {
    console.log('Si puede ver netflix');
} else {
    console.log('No puede ver netflix');
}

// Operador Ternario
console.log(autenticado ? 'Si puede ver netflix' : 'No puede ver netflix');